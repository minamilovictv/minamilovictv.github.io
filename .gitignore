# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*~

# Local env / secrets
.env
.env.local

# Tooling
.claude/

# Backups / archives (keep out of the repo)
*.zip

# Documents ship with the site — the depository links straight at them.
# Only the ones still waiting to be compressed are listed here. Delete a
# line the moment its file is small enough and the depository picks it up.
# Committing a large PDF now and replacing it later leaves BOTH copies in
# git history forever, so it is worth compressing first.
#
# One left, at 16.7 MB. Everything else in assets/documents/ is now
# published; the largest already shipping is annual-report-2021 at
# 10.5 MB, so that is the bar this one needs to clear.
assets/documents/eu-wbf-joint-action-report-2024-2025.pdf

# Internal QA audit — working notes, not part of the published site.
audit/
